# 계획 1 런타임 의존성. 계획 2~5에서 추가된다.
pydantic>=2.13,<3
PyYAML>=6.0,<7
python-dotenv>=1.0,<2
tzdata>=2024.1        # Windows에서 zoneinfo가 tz 데이터베이스를 찾도록

# ── 어댑터 실구현 (계획 2) ── target.adapters="real"일 때만 실제 사용
redis>=5.0                 # redis.asyncio
pymongo>=4.12,<4.17        # AsyncMongoClient (motor는 deprecated) — langgraph-checkpoint-mongodb 요구 상한
aiokafka>=0.14             # group 미참여 assign() 읽기 전용
httpx>=0.28,<1             # REST GET

# ── 조사 엔진 (계획 3) ────────────────────────────────────
langgraph>=1.2,<2
langchain>=1.3,<2
langchain-core>=1.5,<2
langchain-openai>=1.4,<2       # config llm.profiles가 가리키는 OpenAI 호환 게이트웨이용

# ── 순찰 스케줄러 (계획 4) ──────────────────────────────────
APScheduler>=3.11,<4           # 4.x는 알파, API 상이 — 3.x 고정

# ── 케이스 큐·수명주기·영속화 (계획 4b) ──────────────────────
langgraph-checkpoint-mongodb>=0.4,<1   # LangGraph 체크포인터의 Mongo 백엔드

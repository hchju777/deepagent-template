[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    live_llm: 사내 LLM 게이트웨이에 실제로 붙는 테스트(사내망에서만)
    live: 실제 시스템(Redis/Mongo/Kafka/REST)에 붙는 테스트 — 접속 정보가 env에 있을 때만
addopts = -m "not live_llm and not live"

# 선택 — 흐름 그래프의 심볼 층(graphify). 없어도 조사는 돈다(오버레이만 만든다).
# requirements.txt에 안 넣는 이유: 사내 반입 심사가 따로 필요하고(의존성 32개, tree-sitter
# 문법 휠 26개), 사내 PyPI에 없으면 `pip install -r`이 통째로 실패한다. 설치·반입 절차는 README.
# 버전을 고정하는 이유: 우리가 기대는 것이 CLI 플래그(`extract --code-only`,
# `cluster-only --no-label`, `merge-graphs`)와 GRAPH_REPORT.md의 `Token cost` 줄이라 마이너가
# 올라가면 조용히 깨질 수 있다. 올릴 때는 test_진짜_graphify로_코드만_추출한다가 감시자다.
graphifyy==0.9.65

# 0단계 — 무엇을 만드는가

## 한 문장

**대상 시스템(디지털 트윈)을 읽기만 하면서, 이상을 스스로 찾아내고, 원인을
조사해, 증거를 인용한 판정을 내는 에이전트.**

## 왜 "에이전트"여야 하는가

이상 탐지만이면 에이전트가 필요 없다 — 임계값 알람이면 된다. 이 시스템이
LLM을 쓰는 지점은 하나다: **왜 그런지는 미리 적어둘 수 없다.**

> OEE가 512다. 정상 범위는 0~100이다.

여기까지는 규칙이 한다. 그 다음이 문제다. 센서가 틀렸나, 집계 코드가 틀렸나,
설비가 실제로 이상한가, 아니면 계획 데이터가 안 들어왔나? 어느 쪽인지는
**무엇을 더 볼지 정하고, 보고, 다시 정하는** 과정이 있어야 안다. 그 반복이
에이전트다.

## 두 개의 입구, 하나의 엔진

```
사람: "구미 3라인 OEE가 이상해요"  ──┐
                                      ├──▶  [케이스]  ──▶  조사 엔진  ──▶  판정 + 보고서
순찰: 3분마다 점검 → 이상 발견     ──┘
```

입구가 둘인데 엔진은 하나다. **이게 이 설계의 핵심**이다 — 원인을 판정하는
로직이 두 벌이면 둘은 반드시 갈라진다. 사람이 연 케이스와 순찰이 연 케이스는
`origin` 필드 하나만 다르고 나머지 경로는 같다.

## 조사 엔진의 모양

```
frame     가설을 세우고 무엇을 볼지 태스크로 쪼갠다      (LLM)
  ↓
select    실행할 태스크를 고른다 — 병렬 폭·라운드 상한   (코드)
  ↓
execute   서브에이전트가 실제로 데이터를 읽는다          (LLM + 도구)
  ↓
integrate 모은 증거로 가설을 갱신하고 다음을 정한다      (LLM)
  ↓        ├─ continue → select 로 되돌아감 (라운드 반복)
  ↓        ├─ ask      → 사람에게 묻고 멈춤
  ↓        └─ conclude → 판정으로
conclude  인과 사슬(근본 원인 + 기여 요인)을 쓴다        (LLM)
  ↓
verify    인용한 증거 id가 실재하는지 대조한다           (코드, LLM 없음)
```

**LLM이 정하는 것과 코드가 정하는 것이 갈려 있다는 점을 보라.** 라운드를 몇 번
돌지, 몇 개를 동시에 실행할지, 판정이 유효한지는 코드가 쥔다. LLM은 그 울타리
안에서 "무엇이 맞는 판단인가"만 답한다. 재현 가능해야 하고, 상한이 있어야 하고,
감사 가능해야 하는 것은 전부 코드 쪽이다.

## 이 시스템이 절대 안 하는 것

대상 시스템에 **쓰지 않는다**. 이건 정책 문서가 아니라 코드의 성질로 만든다 —
어댑터에 `post`/`put`/`delete` 메서드를 만들지 않으면 "쓰라"고 말하는 것 자체가
표현 불가능해진다. 조치는 사람이 한다. 우리는 권고까지만 한다.

---

# 로드맵

각 단계는 **돌아가는 것**으로 끝난다. 문서만 늘고 실행이 안 되는 단계는 없다.

**이 표가 단계 번호의 단일 진실 소스다.** [README](../README.md)의 진행 표는 같은
목록에 상태 칸만 더한 것이다 — 두 표가 번호를 따로 세다가 실제로 갈렸고(조사 그래프가
한쪽은 8, 다른 쪽은 10이었다), 그 사이에 12단계 "조사 보고서와 이벤트"가 한쪽에서
통째로 사라졌다. 판정이 사람에게 닿는 경로가 번호를 못 받은 채로 한동안 있었다.
그래서 `tests/test_docs.py`가 두 표의 (번호, 이름)이 같은지 검사한다.

| 단계 | 만드는 것 | 끝나면 할 수 있는 것 |
|---|---|---|
| 1 | 뼈대와 규율 (StrictModel, 시계 주입) | `pytest` 통과 |
| 2 | 도메인 모델과 포트 | 실패를 예외 대신 값으로 표현 |
| 3a | config 스키마·로더·기동 검증 | 잘못된 설정이 기동에서 막힘 |
| 3b | 어댑터 (스텁 + 실구현) | **실제 Redis/Mongo/Kafka/REST에서 값을 읽음** |
| 4 | 순찰 프로브 | 정해진 것을 주기적으로 찍는다 |
| 5 | rule 판정과 finding | 이상을 탐지해 finding을 냄 |
| 6a | 케이스 개설과 게이트 | **중복 케이스가 안 열린다** — 첨부한다 |
| 6b | 점검별 주기와 순찰 데몬 | 사람이 안 불러도 돈다 |
| 7 | **사내 LLM 게이트웨이 연결** | 실제 사내 모델이 응답 |
| 8 | 메일 발송 Agent API | 보고서가 사람 받은편지함에 닿음 |
| 9a | 리포트 기간과 시나리오 | 어느 날짜를 세는지가 확정됨 |
| 9b | 수집과 집계 | 숫자가 나옴 — LLM은 숫자에 관여하지 않는다 |
| 9c | 블록 렌더링(HTML) | Outlook 데스크톱에서 안 깨지는 본문 |
| 9d | 추이 차트(표 막대) | 라이브러리 없이 만든 PNG |
| 9e | LLM 서술 | 코드가 센 숫자에 말이 붙음 |
| 9f | `report run` + 메일 배선 | 한 명령이 집계→파일→메일 |
| 9g | 스케줄러(cron·interval) | 상주 프로세스가 스스로 시간을 잼 |
| 10a | 조사 State와 그래프 배선 | **라운드가 상한까지 돎** |
| 10b | frame·integrate (리드 LLM) | LLM이 가설을 세우고 다음 라운드를 정함 |
| 11a | 코드 레포 확보와 지식 층 | 배포된 커밋의 코드를 읽음 (`code status`·`code diff`) |
| 11b | 서브에이전트 3종 | 코드를 읽고 **재계산해 대조함** |
| 11c | 코드 지식 그래프 (graphify) | "어디를 읽을지"를 grep이 아니라 그래프가 답함 |
| 12a | conclude + verify | 인용 검증된 판정이 나옴 |
| 12b | 조사 보고서와 이벤트 | 판정이 사람에게 닿음 |
| 13 | 사람 개입 (질문·재개) | 중간에 묻고 답을 받아 이어감 |
| 14 | 데몬·워커 (lease·resume) | `python -m src patrol run` |
| 15 | 실데이터 E2E·운영 | 실제 이상 → 실제 판정, 정확도 측정 |

아직 안 만든 단계들의 **설계 결정과 기각한 선택지**는 [decisions.md](decisions.md)에 있다.

## 번호 순이 아니라 의존성 순으로 만든다

8·9단계(메일·리포트)가 사업 우선순위로 중간에 끼면서 번호와 실행 순서가 갈라졌다.
남은 것의 실행 순서는 이렇다:

```
4 → 5 → 6a → 6b → 10a → 10b → 11a → 11b → 11c → 12a → 12b → 13 → 14 → 15
└──── 순찰 ────┘   └─────────────── 조사 엔진 ───────────────┘   └ 상주 ┘
```

**순찰을 먼저 했다.** 원래는 엔진이 먼저였는데(케이스가 갈 곳이 있어야 한다는 이유),
10a가 끝나 케이스가 갈 곳이 생겼고 **사람이 구체적인 탐지 시나리오를 줬다.** 그리고
더 큰 이유가 있다 — 엔진을 12단계까지 다 만들고 나서 *실제로 열릴 케이스가 없으면*
그 전부가 헛일이다. 순찰이 진짜 이상을 잡는지 먼저 본 것이 위험을 줄였고, 실제로
**첫날에 진짜 문제 2건을 찾았다**(`Operator/Check`·`Material/ATR Status`).

순찰과 사람은 케이스를 여는 **두 입구**이고 엔진은 하나다 — `origin` 필드만 다르다.
원인 판정 로직이 두 벌이면 둘은 반드시 갈라지므로.

## 앞으로 필요한 정보

3b(대상 시스템)와 7(사내 LLM)에 필요했던 것은 전부 받았다. 남은 것은 조사 쪽이다.

**대상 서비스 코드 (11a)**
- 서비스 목록과 각각이 **어느 레포의 어느 경로**인가 — 한 레포에 여러 서비스가 있는
  경우와 레포마다 하나인 경우가 섞여 있다
- 각 레포의 git URL
- 서비스 간 데이터 흐름 — 누가 무엇을 읽고 무엇을 쓰는가(토폴로지의 재료)
- 사이트별 배포 커밋. `knowledge/deployment/<gbm>/<fct>.json`을 **사람이 수동 유지**하기로 했다

**정상 기준 (5)**
- 어떤 값이 어느 범위를 벗어나면 이상인가 — rule의 재료

# 10b단계 — 리드 LLM (frame · integrate)

> **목적**: 10a가 세운 울타리 안에 LLM을 넣는다. **울타리는 한 줄도 안 바꿨다** —
> 10a에서 미리 갈라 둔 자리(`EngineDeps.frame`/`integrate`)에 꽂기만 했다.
> 나중에 얹는 방어는 그 사이에 난 구멍을 못 막는다.

```bash
python -m src case investigate c-1 --stub-seeds examples/stub-seeds.json
```

```
  mx/gumi  c-1 — Operator/Check — 전부 0 — alarm·caution·normal이 모두 0이다
  gauss-o-flash(id=339) → https://… [인증: 헤더 셋(사내 게이트웨이), TLS: 시스템 저장소]
  라운드 2 — 끝난 이유: decision

  가설
    h-1 [refuted] 파생 집계가 비어 있다  (t-2.e1)

  태스크
    ✅ t-1 어떤 컬렉션이 있는지 본다 — mongo.list_collections → 1건 ['bb_state']
    ✅ t-2 찾은 컬렉션을 읽는다 — mongo.find collection='bb_state' filter={} limit=3 → …

  증거 2건
    t-1.e1  mongo.list_collections
    t-2.e1  mongo.find collection='bb_state' filter={} limit=3
```

**`t-1` → `t-2`를 보라.** 리드가 컬렉션 이름을 **모르는 채로** 시작해서, 먼저 목록을
찾고, 찾은 것을 읽었다. `t-2`는 `input_evidence_ids: ["t-1.e1"]`을 달고 나왔으므로
10a의 게이트가 붙잡았다가 2라운드에 풀었다 — 이 단계가 만든 것의 요약이다.

## 무엇을 LLM이 정하고 무엇을 코드가 쥐는가

| | |
|---|---|
| **LLM** | 가설 내용 · 무엇을 볼지 · 라운드마다 continue/conclude |
| **코드** | 라운드 상한 · 병렬 폭 · 실행 가능 판정 · 태스크 개수 상한 · 수명주기 소독 · **증거 인용 검사** · 등재제 |

규율 6의 기준 그대로다: 재현 가능해야 하고, 상한이 있어야 하고, 감사 가능해야 하는
것은 코드가 쥔다. 그 안에서 "무엇이 맞는 판단인가"만 LLM에게 맡긴다.

## 사람이 "무엇을 볼지"를 미리 다 적지 않는다

리드가 `mongo.find(collection=…)`를 부르려면 컬렉션 이름을 알아야 한다. 그걸 사람이
config에 적어 주면 **조사는 우리가 적어 준 곳만 본다** — 우리가 아는 만큼만 아는
에이전트다([decisions ⑮](decisions.md)).

그래서 이름을 주지 않고 **찾는 읽기**를 준다:
`mongo.list_collections` · `kafka.list_topics` · `redis.scan`.
등재된 10가지 중 REST 하나만 사람이 선언한다 — 그건 POST라서 body가 나가기 때문이고,
나머지 9개는 인자가 자유다(규율 9의 "메서드 수준에서 잃은 것의 대체물이 body 수준의
닫힌 스키마다"와 같은 선).

프롬프트에도 이름을 안 적는다. `{actions}` 자리는 **`domain/actions.py`의 등재표와
그 사이트의 `infra.rest.entries`에서 생성한다**(`briefing.action_catalog`). 손으로
적으면 config와 갈라지고, **갈라진 쪽이 곧 LLM이 믿는 세계**가 된다 — 항목을
추가했는데 프롬프트를 안 고치면 리드는 그게 있는 줄도 모른다.

Kafka가 없는 사이트에는 `kafka.*`가 목록에 아예 안 나온다. 실행기가 거부하므로 사고는
안 나지만 **라운드 하나가 통째로 낭비되고**, 상한이 4라운드면 그게 25%다.

## 프롬프트에 접속 정보가 안 섞인다

url·비밀번호·계정은 브리핑에 들어가지 않는다. 리드가 알아야 하는 것은 "무엇을 부를
수 있는가"이지 "어디에 붙어 있는가"가 아니다 — 후자는 어댑터의 일이다.

**사내 게이트웨이가 프롬프트를 어떻게 보관하는지 우리는 모른다.** 모르는 곳에
비밀번호를 보내지 않는 것이 유일하게 지킬 수 있는 규칙이다.
`tests/application/test_briefing.py`가 지키고, 사내에서는
`test_리드_프롬프트에_접속_정보가_안_실린다`가 **진짜 config로** 다시 본다.

## 실패가 조용하면 안 된다

LLM이 죽거나 JSON이 안 나오면 태스크가 0개가 되고, 그러면 10a의 `no_runnable`로
끝난다 — **"조사했는데 아무것도 안 했다"가 "조사할 게 없었다"와 같은 모양이 된다.**
5단계의 `unreachable` 문제와 정확히 같다.

그래서 `stopped_by`에 네 번째 값 `llm_error`를 더하고 `llm_errors`에 사유를 쌓는다.

- `frame`이 실패하면 **라운드를 시작하지 않고 END로 간다**(`route_after_frame`).
  흘려보내면 select가 0건 → integrate가 LLM을 **또** 부르고, 또 죽고, 끝난 이유가
  `no_runnable`로 덮인다.
- `integrate`가 실패하면 그 이유가 **상한보다 먼저** 읽힌다. 순서가 반대면
  "LLM이 죽어서"가 "상한에 걸려서"로 둔갑한다.

12a가 이 값을 보고 "미확정"과 "조사 실패(degraded)"를 가른다.

## 재시도는 한 번뿐이다 (`RETRIES = 1`)

파싱 실패는 모델이 형식을 놓친 것이라 한 번 더 물으면 대개 된다. 두 번 이상은 같은
실패를 반복하며 라운드 시간만 늘린다 — 조사는 라운드마다 LLM을 부르므로 재시도가
길면 전체가 늘어진다. 테스트가 **양쪽으로** 고정한다: 재시도를 지워도 실패하고,
2로 늘려도 실패한다.

## 관용 범위를 한 곳에 모았다 (`schemas.py`)

코드펜스를 벗기고, 앞뒤에 붙은 말이 있으면 첫 `{`부터 마지막 `}`까지 다시 시도한다.
"네, 아래와 같습니다:"는 형식 위반이지 답이 틀린 것이 아니다 — 거부하면 **내용은
정확한 답**이 형식 때문에 버려지고 그 라운드가 빈손이 된다.

리드와 11b의 서브에이전트가 **같은** 범위를 써야 한다. 각자 벗기면 한쪽은 코드펜스를
받아 주고 다른 쪽은 안 받아서, 같은 모델이 같은 응답을 줬는데 노드마다 다르게 동작한다.

검증은 `StrictModel`이므로 모델이 지어낸 필드(`"confidence": 0.9`)는 **응답 전체가
거부된다**(규율 5). 받아 주면 아무도 안 쓰는 값이 State에 섞이고, 보고서에는 실릴 수
있다.

## 새로 세운 경계 — 증거 인용 검사 (규율 3)

리드가 가설에 적은 증거 id는 **환각일 수 있다.** 그냥 들이면 다음 라운드의 브리핑이
`<가설>` 블록에 그 id를 실어 보내고, 리드는 **자기가 지어낸 id를 근거로 다시
추론한다.** 라운드마다 복리로 불어나는 종류의 오류라, 루프가 생긴 지금 막아야 한다.

`_accept_hypotheses`(nodes.py — `_sanitize_task`와 같은 경계)가:

1. State에 실재하지 않는 인용을 걷어낸다.
2. 인용이 **하나도 안 남으면** 판정을 `open`으로 되돌린다 — 근거가 전부 사라진
   "supported"는 근거 없는 단정이고, 그게 12a의 최종 판정 재료가 된다.
   남은 근거가 있으면 판정은 유지한다.
3. 걷어낸 사실을 `llm_errors`에 남긴다. **조용히 고치면 프롬프트가 안 먹히고 있다는
   것을 아무도 모른다.**

`lead.py`가 아니라 `nodes.py`에 둔 이유: 11b의 서브에이전트 경로가 생겨도 같은
경계를 지나야 한다. 소독이 `frame`/`integrate` 구현마다 있으면 언젠가 하나가 빠진다.

## 종료 코드는 등급이 다르다

조사가 아예 못 돈 것(`stopped_by == "llm_error"`)만 `1`이다. "돌긴 했는데 리드가 없는
증거를 인용했다"는 `0`이되 stderr로 시끄럽게 알린다. 후자까지 1로 주면 **"빨간불이
원래 그렇다"가 되고, 그러면 진짜 빨간불도 안 보이게 된다.**

## 프롬프트 자리 검사 — 두 방향 다 막는다

`_load_lead_prompt`가 기동 때 한 번 본다.

- `{case}`·`{actions}`가 **없으면**: 리드는 무엇을 부를 수 있는지 모른 채 지어낸다.
  증상은 "조사가 항상 빈손"이고 원인은 안 보인다.
- 오타 난 `{max_round}`처럼 **우리가 안 채우는 자리**가 있으면: 그 `{...}`가
  치환되지 않은 채 LLM에게 나간다. 9e에서 `{max_chars}`가 실제로 그랬고 **리포트는
  정상으로 보여서 아무도 못 봤다.**

자리 목록(`briefing.FRAME_SLOTS`/`INTEGRATE_SLOTS`)이 실제로 채우는 것과 같은지도
테스트가 본다 — 갈라지면 검사가 거짓말을 한다.

`str.format`은 **쓰지 않는다.** 프롬프트에 JSON 예시가 들어 있어 `{`가 널려 있고,
`format`은 그걸 자리로 읽어 `KeyError: '\n  "hypotheses"'`로 죽는다. 이번에도 실제로
죽었고, 9e가 같은 함정을 겪고 `report/comment.py`에 주석까지 남겨 뒀는데도 다시 밟았다.

## 한 줄이 한 증거다

증거·가설·태스크 블록은 개행을 눕혀서 싣는다. 대상 데이터는 여러 줄이 정상인데 날것
으로 실리면 블록에 **가짜 항목**이 생기고, 리드는 있지도 않은 증거 id를 인용한다.

`runner_probe._summarize`가 `repr`로 이미 이스케이프하지만 블록에서 한 번 더 막는다.
`EvidenceRef`는 어디서나 만들 수 있고(11b가 곧 만든다), **"생산자가 다 지킨다"는
가정은 생산자가 늘어나면 깨진다.**

## 이 단계에서 실제로 걸린 것

### 내가 쓴 docstring이 없는 검사를 주장했다

`fill`의 설명에 "**채운 뒤에 남은 자리를 검사한다**"라고 적어 놨는데 코드에는 그
검사가 없었다. CLAUDE.md가 경고하는 바로 그 형태다 — 문서가 주장하는 배선을 확인
없이 믿으면, 다음 사람은 그게 있다고 믿고 그 위에 얹는다.

검사를 **기동 시점**(`_load_lead_prompt`)에 실제로 만들고 docstring을 사실로 고쳤다.
매 라운드가 아니라 기동인 이유: 오타 난 자리는 라운드마다 똑같이 남으므로 매번 볼
이유가 없고, 조사가 시작된 뒤에 발견해 봐야 늦다.

### 내 테스트 하나가 거짓 초록이었다

"증거 요약에 개행이 없다"를 **이미 이스케이프된 `\n`**(백슬래시 + n 두 글자)으로
검사했다. 그건 아무것도 검사하지 않은 것이다 — 방어를 지워도 통과했다. 진짜 개행을
넣도록 고쳤고, 덤으로 **실행기가 실제로 낸 증거**로도 확인한다(소비자로 직접 검증).

### 코드펜스 테스트가 `strip_fence`를 고정하지 못했다

RED 확인에서 `strip_fence`를 통째로 무력화했는데 **테스트가 전부 통과했다.**
`parse_object`의 "첫 `{`부터 마지막 `}`까지" 재시도가 울타리째 감싼 JSON도 구해
주기 때문이다. 두 겹은 독립이므로 각각 고정하도록 테스트를 더했다.

## 검증

`pytest tests/` — 963개. 10b와 그 보정이 더한 것은 `tests/application` 73개.
**방어를 하나씩 실제로 지워 전부 RED를 봤다**:

| 지운 것 | |
|---|---|
| `ask_json` 최외곽 try/except | 1 failed |
| 재시도(`RETRIES=0`) / 재시도 늘리기(`=2`) | 2 / 1 failed |
| 태스크 수명주기 소독 | 1 failed |
| 가설 증거 인용 검사 | 2 failed |
| 인용 기록(`llm_errors`) 생략 | 3 failed |
| 근거가 다 사라져도 판정 유지 / 무조건 open | 1 / 1 failed |
| `route_after_frame` (그냥 흘려보낸다) | 1 failed |
| integrate의 `stopped_by` 우선권 | 1 failed |
| 등재 목록을 손으로 적기 | 3 failed |
| 등재 목록에 base_url 싣기 | 1 failed |
| 케이스 블록에 토픽 이름 적기 | 1 failed |
| 블록의 개행 방어 | 2 failed |
| 코드펜스 벗기기 | 1 failed |
| `StrictModel` → `BaseModel` | 1 failed |
| 프롬프트 자리 검사 | 1 failed |
| 종료 코드 등급(전부 1 / 전부 0) | 1 / 1 failed |

보정(①~⑤)에서 지운 것 18가지도 전부 RED를 봤다:

| 지운 것 | |
|---|---|
| 곁다리 걷어내기 / 걷어낸 뒤 재검증 / 걷어낸 사실 기록 | 3 / 1 / 1 failed |
| 수리 사유가 걷어낸 키까지 싣기 | 1 failed |
| 수리 재시도 / 사유를 앞에 붙이기 | 1 / 2 failed |
| 이름 추측 기록 / 기록 대신 거부 / 인자 범위 확대 / 대본에도 켜기 | 1 / 1 / 1 / 2 failed |
| 예시를 손으로 적기 / frame 예시에 이름 넣기 | 1 / 1 failed |
| integrate 자리표시자를 완결된 값으로 / 가설 하나만 | 1 / 1 failed |
| `{example}` 필수 검사 | 1 failed |
| 트레이스 훅 / 예외 흡수 / 실패한 시도 생략 | 2 / 1 / 2 failed |

사내 케이스에서 찾은 버그를 고치고 또 7가지:

| 지운 것 | |
|---|---|
| 이미 있는 id 거부 / 기존 State를 안 봄 / 거부 기록 | 3 / 2 / 2 failed |
| 예시 id를 항상 1부터 / 빈 번호 메우기 / priority를 번호에 묶기 | 1 / 1 / 1 failed |
| 결정 지침을 예시 앞으로 **실제로 옮김** | 1 failed |

마지막 것은 처음에 제목만 바꿔서 거짓 초록이 났다 — **절을 진짜로 옮겨야** 빨간불이 온다.

무raise는 **실제로 던지는** `ExplodingAdapter`로 본다. `ScriptedAdapter`는 예약된
예외를 `status="error"` 응답으로 바꿔 주므로 "어댑터가 계약대로 실패를 값으로 돌려준
것"이고, 그것만으로는 `ask_json`의 try/except를 지워도 전부 초록이다 — 9e에서
같은 거짓 초록이 실제로 났다.

## 사내에서 한 번 돌려야 하는 것

오프라인 테스트가 잠그는 것은 **우리 코드가 무엇을 받아들이나**다. 사내 모델이 그
형식을 실제로 지키는지는 붙어 봐야 안다.

```powershell
.venv\Scripts\python.exe -m pytest tests\live -m live_llm -v -s
```

**`-s`가 없으면 통과한 테스트의 출력이 안 보인다.** 여기서 보려는 것의
절반이 그 출력이다 — 처음 적은 명령에 이게 빠져 있었다.

| 테스트 | 빨간불이면 |
|---|---|
| `test_리드가_실제로_조사_계획을_낸다` | **10b가 사내에서 안 돈다.** 프롬프트를 고쳐야 한다 |
| `test_리드가_라운드_하나에_여러_시스템을_본다` | ①이 안 먹혔다 — 예시 말고 다른 것이 문제다 |
| `test_리드가_예시의_자리표시자를_그대로_베끼지_않는다` | 예시 밖에서 이름을 지어냈다 |
| `test_리드가_등재된_action만_고른다` | 지어낸 action이 라운드를 낭비한다 — 목록 제시를 강화 |
| `test_리드가_이름을_찍지_않고_먼저_찾는다` | "추측하지 마라"가 안 먹힌다 — ⑮ 설계의 전제가 흔들린다 |
| `test_리드_프롬프트에_접속_정보가_안_실린다` | **진짜 비밀번호가 게이트웨이로 나간다** |

## 사내에서 한 번 재 보고 고친 것 (10b 보정)

처음 사내에서 돌려 보니 리드가 이렇게 답했다:

```
  가설 1개 · 태스크 1개
    t-1  MongoDB에 존재하는 컬렉션 목록을 확인한다 — mongo.list_collections {}
```

프롬프트의 예시와 나란히 놓으면 무슨 일이 있었는지 보인다:

```jsonc
// 예시
{ "id": "t-1", "goal": "무엇을 확인하는가 (한국어)", "role": "data_prober",
  "action": "mongo.list_collections", "params": {}, "priority": 10 }
```

**`id`·`action`·`params`가 그대로다. `goal` 문장 하나만 자기 말로 바꿨다.**

사내 모델은 Haiku·낮은 effort 급이다. 그 등급에서는 **판단해서 고르는 것이 아니라
주어진 틀을 채운다.** 예시에 태스크가 하나였으므로 하나를 냈고, `input_evidence_ids`가
없었으므로 안 썼다. "태스크 하나는 읽기 하나다", "앞선 결과가 필요하면
`input_evidence_ids`에 적어라" 같은 **산문 규칙은 아무 효과도 못 냈다.**

틀린 답을 낸 것이 아니라는 점이 중요하다 — `mongo.list_collections`는 정확히 맞는
첫 수였다. 문제는 그게 **판단의 결과가 아니었다**는 것이다. 그래서 고치는 방법도
다르다: 설득하는 게 아니라 **베낄 것을 제대로 놓아두면 된다.** 예측 가능하게 약한
모델은 오히려 조종하기 쉽다.

### ① 예시가 곧 출력이다 — 그래서 예시를 코드가 만든다

예시는 "모양을 보여 주는 것"이 아니라 **우리가 원하는 첫 수 그 자체**여야 한다.

| | |
|---|---|
| frame 예시 | **이름이 필요 없는 읽기만** 2~3개 + 가설 2개 |
| integrate 예시 | 증거에서 얻은 이름으로 읽는 태스크 + `input_evidence_ids` |

frame에서 이름을 빼는 이유가 핵심이다. **모델은 `params` 값도 그대로 베낀다.**
예시에 `"collection": "..."`를 두면 진짜로 `"..."`를 조회하고, 빈 결과가 "데이터가
없다"로 읽힌다 — ⑮가 경고하는 바로 그 오독이다. 반대로 integrate 시점엔 증거 블록에
**진짜 이름이 이미 있으므로** 틀을 베껴도 채워 넣는 값이 진짜다. 그래서 라운드 구조가
의존성을 진다.

예시도 `{actions}`와 같은 등재표에서 생성한다. Kafka 없는 사이트에 `kafka.list_topics`가
예시로 박혀 있으면 모델은 **그걸 그대로 부른다.**

integrate 예시의 자리표시자는 `"위 증거에서 본 컬렉션 이름"`처럼 **지시문 모양**이다.
frame 관측에서 지시문 모양(`"무엇을 확인하는가 (한국어)"`)은 바뀌었고 완결된 값
(`params: {}`)은 베껴졌다 — 그 차이를 그대로 이용한다.

### ② 수리 재시도

같은 프롬프트를 한 번 더 보내는 것은 틀을 채우는 모델에겐 재시도가 아니다.
같은 틀을 주면 같은 답이 온다. 이제 **실패 사유를 맨 뒤에 붙여** 다시 묻는다
(모델은 마지막에 읽은 지시를 더 따른다 — 9e의 울타리가 <사실> 뒤에도 한 겹 있는 것과
같은 이유). 사유에는 **이미 걷어낸 키를 싣지 않는다** — 엉뚱한 곳을 고치게 된다.

### ③ 곁다리 키는 걷어내고 기록한다 — 규율 5의 좁은 예외

`"confidence": 0.9` 하나로 응답 전체를 버리면 **멀쩡한 조사 계획이 라운드째 날아간다.**
약한 모델에겐 흔한 일이고, 곁다리는 형식 실수이지 계획이 틀린 것이 아니다.

`extra_forbidden`**만** 골라 걷어내고 다시 검증한다. 값이 틀린 것(`"decision": "maybe"`)은
여전히 실패이고 ②의 수리 재시도로 간다. 걷어낸 키는 `llm_errors`에 남는다.

`StrictModel`은 config와 도메인 객체에 그대로다 — 거기서 모르는 키는 **사람이 잘못
적은 것**이고 시끄럽게 죽는 쪽이 맞다. 예외는 LLM 응답 봉투 하나뿐이다.

### ④ "이름을 찍지 마라"를 계측한다

프롬프트의 부탁이 실제로 먹히는지 아무도 몰랐다. 이제 태스크가 `collection`·`topic`·
`key`·`group`(`DISCOVERED_ARGS`)에 **증거에 없는 값**을 대면 `llm_errors`에 남는다.

**막지는 않는다.** 증상 자체가 이름을 담은 정당한 경우가 있고, 무엇보다 빈도를 알아야
막을지 정할 수 있다. `entry`가 빠진 것은 우연이 아니다 — REST 등재 항목은 config가
선언하므로 찾을 것이 없다. `pattern`도 빠진다 — `*`가 정상적인 값이다.

`case dryrun`은 이 검사를 끈다(`EngineDeps.check_discovery=False`). 대본은 시스템을
아는 사람이 이름을 알고 적은 것이라, 켜 두면 기록이 거짓 양성으로만 찬다.

### ⑤ 트레이스 — **이게 없어서 위 넷이 전부 추측이었다**

```bash
python -m src case investigate c-1 --trace
#  → output/traces/c-1/01-r0-frame.md ...
```

라운드마다 **프롬프트 · 날것 응답 · 결과**가 남는다. 실패한 시도도 남는다 — 고칠 근거가
거기 있다. "모델이 예시를 그대로 베낀다"는 사실은 **최종 State에는 흔적이 없었다**
(파싱된 결과는 멀쩡해 보인다). 10b를 끝낼 때 이 창이 없었던 것이 진짜 누락이다.

날것 응답에는 대상 데이터가 실릴 수 있다. `output/`은 `.gitignore`에 있다.

### 사내 케이스로 돌려 보고 찾은 버그 — **최종 State가 거짓말을 했다**

①이 먹힌 뒤 실제 케이스(`Operator/Check`)로 `--trace`를 돌렸더니 이런 출력이 나왔다:

```
라운드 4 — 끝난 이유: max_rounds
태스크
  t-4  alarm 컬렉션에 저장된 데이터를 확인한다 — (실행 안 됨)
  t-5  Kafka 토픽의 최근 메시지를 확인한다   — (실행 안 됨)
증거
  t-4.e1  mongo.find collection='alarm' …      ← 실행 안 됐는데 증거가 있다?
  t-5.e1  kafka.tail topic='…' …
```

재현했더니 실행기는 `[t-1, t-2, t-3, t-4, t-5, t-4, t-5, t-4, t-5]`를 돌렸다 —
**같은 읽기가 세 번씩**, 라운드 4개 중 둘이 통째로 반복이었다.

원인은 ①과 같은 성질이다. **리드가 예시의 id까지 그대로 베낀다.** integrate 예시가
`t-4`·`t-5`였으므로 매 라운드 같은 id를 냈고 →`_sanitize_task`가 수명주기를
초기화하고 → 리듀서가 **완료된 태스크를 `pending`으로 덮어썼다.** 증거는 별도
리스트라 살아남아서 저 모순이 남았다.

규율 4의 구멍이다. 소독은 **새 태스크**에는 맞지만 이미 끝난 id에는 재앙이다.
12b의 보고서가 이 State를 그대로 쓰면 **안 한 일을 했다고, 한 일을 안 했다고** 적는다.

고친 것 둘:

- **`_accept_tasks`가 이미 있는 id를 안 받는다**(`taken`). 리드는 완료된 태스크를
  되살리거나 고쳐 쓸 수 없다. 거부한 사실은 `llm_errors`에 남는다.
- **예시의 id를 다음 빈 번호부터 매긴다**(`next_task_number`). 막기만 하면 3라운드부터
  새 태스크가 0이 되어 조사가 얕아진다. **베끼는 성질과 싸우지 말고 이용한다** —
  매 라운드 다음 번호를 보여 주면 베껴도 새 id가 된다. 라운드 2는 `t-4`·`t-5`,
  라운드 3은 `t-6`·`t-7`.

고친 뒤 같은 시나리오: 서로 다른 읽기 9개, **중복 0건**, 모든 태스크가 제 상태를 적는다.

덤으로 `priority`를 번호에서 떼어 **라운드 안의 순서**로 바꿨다. 번호를 곱하면
라운드가 깊어질수록 우선순위가 커져(늦어져) 앞 라운드의 잔여 태스크에 계속 밀린다 —
정작 지금 제일 궁금한 읽기가 제일 나중이 된다.

### 이 모델은 스스로 `conclude`하지 않는다

같은 원인이다 — 예시의 `"decision": "continue"`를 베낀다. 그래서 조사가 매번
`max_rounds`로 끝났다. 예시에 `conclude`를 넣으면 반대로 항상 끝내 버리므로 값은
그대로 두고, **결정 지침을 예시 뒤로 옮겼다**(모델은 마지막에 읽은 지시를 더 따른다).

이것도 안 먹히면 그대로 받아들이면 된다 — `stopped_by`가 `max_rounds`인지
`no_runnable`인지 `decision`인지를 정직하게 적고, 12a가 그걸 읽는다. **"끝낼 때를
아는 것"은 코드가 이미 쥐고 있다.**

### 보정에서도 거짓 초록 셋이 나왔다

RED 확인에서 방어를 지웠는데 테스트가 통과한 것이 셋이었다:

| | 왜 |
|---|---|
| `_extra_paths`의 조기 반환 | **방어가 아니라 지름길이었다.** 걷어낸 뒤 재검증이 어차피 잡는다. 빼니 수리 메시지가 오히려 정확해졌다(이미 걷어낸 키를 안 탓한다) |
| `dryrun`의 `check_discovery=False` | 플래그 **동작**만 테스트하고 dryrun이 실제로 끄는지는 안 봤다 |
| `{example}` 필수 검사 | 테스트가 `{case}`·`{actions}`까지 빠진 프롬프트를 써서, `example`을 빼도 다른 것이 먼저 걸렸다 |

앞의 것은 코드를 지웠고, 뒤의 둘은 테스트를 고쳤다.

## 검토 포인트

1. **`max_rounds=4` × 라운드당 LLM 호출 1~2회 × 사이트 28개.** 케이스가 몰릴 때의
   비용이 여기 걸린다. 14단계의 워커가 동시 실행을 조일 자리이기도 하다.
2. **인용 환각이 얼마나 나오는가.** `llm_errors`가 매번 차면 프롬프트를 고치는 것이
   맞고, 안 차면 이 경계는 조용히 서 있기만 하면 된다.
3. **`note` 필드.** 모델이 설명을 덧붙이고 싶어 하는 자리로 뒀다(없으면 지어내서 다른
   칸에 넣는다). State에는 안 들어간다 — 12a가 쓸 일이 있는지 봐야 한다.
4. **리드는 아직 "무엇을 조사할지" 모른다.** 사내 케이스에서 컬렉션 22개·토픽
   **179개**·Redis 키 200+(잘림)를 찾아 놓고, 그중 `alarm` 컬렉션을 고른 근거는
   **이름이 alarm이라서**였다. 어느 서비스가 무엇을 쓰는지, 그 토픽이 배지와 무슨
   관계인지 아는 바가 없다. **11a·11c가 메우는 것이 정확히 이 구멍이고**, 179라는
   숫자가 그 우선순위를 정한다.
5. **`role`은 아직 안 읽힌다.** `ProbeRunner`는 `action`만 본다. 11b에서 서브에이전트
   3종이 생기면 그때 갈린다.

→ 다음: 11a — 조사 대상 코드 레포 확보와 지식 층. 리드가 "데이터가 이상하다"까지는
가는데 "왜 그런가"는 코드를 봐야 답이 나온다.
